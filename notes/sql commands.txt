sqlplus
 system
password
->create a table: 
create table transaction_2024(transaction_id number not null, transaction_date date not null, transaction_to varcha
r(255) not null, transaction_amount number(10) not null, transaction_remarks varchar(255) not null);

->structure check
describe transaction_2024

adding primary key without sequence
alter table transaction_2024 add primary key(transaction_id);

creating sequence
 create sequence transaction_seq start with 2024001 increment by 1;

drop column
alter table transaction_2024 drop column transaction_id;

->add column
alter table transaction_2024 add transaction_id number not null;

->adding primary key with sequence
alter table transaction_2024 add constraint transaction_seq primary key(transaction_id);


syntax of trigger
time >>before, after
event  >>insert,update,delete

syntax
create or replace 
















any reflection on base table how it impact on view
explore intellij
access specifier at variable level class level

main=>interpreter looks for structure/this kind of argument 
physical dependency

 
lombok->c>users>xxpoojas>m2>repository>org>projectlombok>lombok>1.18.22&1.12.2
java c compiler=> maven goals>not play button>its on maven>mvn clean install>source and test file copiles.
project without maven and with maven>

what happens if it is default how it can be accessed->what is access specifier
access specifier at class level and try it out
store jar in some other location
run to through command prompt---why to clickbuild and the run 

